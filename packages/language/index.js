import grammar from "./grammars/stack.tmLanguage.json" with { type: "json" };
import configuration from "./language-configuration.json" with { type: "json" };

export const stackLanguage = grammar;
export const languageConfiguration = configuration;

export default stackLanguage;
